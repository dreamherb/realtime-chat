<!DOCTYPE html>
<html lang="ko">
  <%- include('include/header', { title: "알림 설정" }) %>

  <body>
    <main class="page">
      <section class="section">
        <h1 class="logo">알림 설정</h1>

        <div class="card card--auth notifications-card">
          <div class="notifications-card__section">
            <div class="notifications-card__head">
              <h2 class="notifications-card__title">계정 알림 설정</h2>
              <label class="switch">
                <input type="checkbox" id="accountToggle" aria-label="계정 알림" />
                <span class="switch__ui"></span>
              </label>
            </div>
            <p class="muted notifications-card__desc">
              이 계정이 알림을 받을지 정합니다. 켜 두면 로그아웃 후에도 유지되고,
              다른 기기에서 로그인하면 그 기기로도 푸시가 갈 수 있습니다.
            </p>
            <p class="notifications-card__status" id="accountStatus"></p>
          </div>

          <hr class="notifications-card__divider" />

          <div class="notifications-card__section">
            <h2 class="notifications-card__title">기기 알림 허용</h2>
            <p class="muted notifications-card__desc">
              PC 브라우저와 모바일 앱(PWA)은 알림 권한이 각각 있습니다.
              이 페이지에서는 바꿀 수 없고, 지금 보고 있는 기기의 허용 상태만 표시합니다.
            </p>
            <p class="notifications-card__status" id="deviceStatus"></p>
          </div>

          <div class="row row--gap-sm" style="margin-top: 1.5rem">
            <a class="btn btn--secondary" href="/dashboard">대시보드로 돌아가기</a>
          </div>
        </div>
      </section>
    </main>

    <%- include('include/footer') %>
    <div class="modal-overlay" id="alertModalOverlay">
      <div class="modal" id="alertModal">
        <h2>Notice</h2>
        <p id="alertModalMsg"></p>
        <button onclick="closeAlertModal()">확인</button>
      </div>
    </div>

    <script>
      window.__NOTIFICATIONS__ = {
        pushConfigured: <%- JSON.stringify(Boolean(pushConfigured)) %>
      };
    </script>
    <script src="/js/common.js"></script>
    <script src="/js/notifications-settings.js"></script>
  </body>
</html>
