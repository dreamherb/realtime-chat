services:
  app:
    build: .
    restart: unless-stopped
    stop_grace_period: 20s
    command: ["node", "app.js"]

  worker:
    build: .
    restart: unless-stopped
    stop_grace_period: 20s
    command: ["node", "workers/chat-message.worker.js"]
